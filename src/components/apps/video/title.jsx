import React from 'react';
import { Component } from 'react';

export default class Title extends Component {
    render() {
        return (
            <div>
                <h1>React Tutorial Video Search</h1>
                <p>React is a JavaScript library that was built by Facebook. It has seen a meteoric rise in the last year.</p>
            </div>
        );
    }
}